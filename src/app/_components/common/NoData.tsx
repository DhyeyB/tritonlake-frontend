const NoData: React.FC = () => {
    return (
        <div className={`mt-5 mb-5 mx-auto text-center`}>
            <span className="text-center fw-bold text-muted fs-2">No records found</span>
        </div>
    )
}

export default NoData
